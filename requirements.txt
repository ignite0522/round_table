# Core round-table logic uses the standard library. The GUI requires Flask,
# while real knights shell out to Codex CLI (`codex exec`).

# --- runtime ---
Flask>=3.0.0

# --- dev ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
